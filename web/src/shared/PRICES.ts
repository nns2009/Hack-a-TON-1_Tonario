import {toNano} from "ton";

const VIEW = toNano(0.0001);
const LIKE = toNano(0.01);
const FIRE = toNano(0.1);
const DIAMOND = toNano(1);

export default {
    VIEW,
    LIKE,
    FIRE,
    DIAMOND
}
